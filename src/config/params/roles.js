module.exports = {
  roles: {
    // site admin (owner or similar), unrestricted access to almost anything
    admin:   'ADMIN',

    // internal users (employees, agents)
    manager: 'MANAGER',

    // end users
    user:    'USER'
  }
};
